<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rightside.doutorja">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icone"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EspecialidadeActivity" />
        <activity android:name=".EspecialistaActivity" />
        <activity android:name=".EspecialistaEscolhidoActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".FirebaseActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DiaDaSemanaActivity" />
        <activity android:name=".HoraActivity"></activity>
    </application>

</manifest>